[pytest]
testpaths = tests
strict_markers = true
markers =
    smoke: fast, offline checks that the service boots and answers — run on every change
    integration: real local resources (sqlite, filesystem), no network — opt in with --integration / INTEGRATION_TESTS=1
    live: real external services (Render API, auth-service box) — opt in with --live / LIVE_TESTS=1
